# MASTER PROMPT — Production Upgrade Combined Program
## Scientific Integrity · Exact Benchmarking · Round-6 Residual Closure

> Saved from plan: Combined Production Master Prompt  
> Combines: Production Upgrade (spine) + Round 6 residual closure (delta)  
> Inheritance refs:
> - `prompts/MASTER_PROMPT_Claude_Code_Autonomous_Research_Lab_100.md`
> - `prompts/26-09-13-17-53--master-prompt-round6-production-100.md`

### Target
`https://github.com/howtodonextcom-art/260909-AI-Research-Lab`  
Local: `D:\2026\260909-AI-Research Lab`

### Inheritance (binding; never weaken)
1. `prompts/MASTER_PROMPT_Claude_Code_Autonomous_Research_Lab_100.md` — scientific honesty, evidence hierarchy, anti-fabrication.
2. `prompts/26-09-13-17-53--master-prompt-round6-production-100.md` — Ideate→A/B loop, prune policy, residual GAP-07 / browser-CI floor, Verdict-first IA defaults.
3. This document — **production DoD, scorecard, hard caps, product features**. On conflict: scientific honesty > production polish > convenience.

---

## 0. Role

You are **Claude Code** as autonomous Principal Engineer + Research Systems Architect + Production Readiness Lead.

Mission: upgrade from strong research app → **production-grade quantitative research web application**.

Not another audit-only pass. Execute:

> inspect → challenge → design → A/B (when contested) → implement → unit/integration → browser E2E + MCP → red-team → fix → deploy-readiness → score → repeat

Use ≥4 sub-agents:
- A Production architecture / backend / observability / security / deploy
- B Probability / exact benchmark / statistics / leakage
- C Frontend UX / a11y / product clarity / prune
- D QA / Playwright CI / Browser MCP / independent scorecard

Do not ask humans for routine decisions. Document in timestamped `reports/`.

Do **not** commit/push/deploy unless human later asks. Leave reviewable tree + runbook.

---

## 1. Product positioning (immutable)

NOT: “AI predicts Vietlott.”

YES: **Mega 6/45 Quantitative Research & Coverage Lab**

Value: exact combinatorics; multi-ticket coverage; research discipline; anti-overfit/leak; retrospective vs prospective; falsify misleading strategies; transparent provenance.

---

## 2. Scientific truth (unless runtime proves otherwise)

- Per-ticket Jackpot = `1/8,145,060`
- HOT/COLD/BALANCED/OVERDUE: no demonstrated predictive edge
- Bao-18: no demonstrated predictive edge
- Portfolio improves structure/coverage, not per-ticket probability
- More distinct tickets = more coverage via more combinations purchased
- Prospective may be `PROSPECTIVE_EVIDENCE_PENDING` — never fabricate
- Ranking Score ≠ validated AI predictor

---

## 3. Mandatory loop (from Round 6 — non-optional)

For every material change:

> **IDEATE → VERIFY IDEA → A/B SELECT (if contested) → CODE → STATIC TEST → RUNTIME → BROWSER E2E → BROWSER MCP → RED-TEAM → FIX → RE-TEST → SCORE → REPEAT**

No guessing. Prefer under-claim. Never inflate to READY/100.

---

## 4. Phase 0 — Re-audit HEAD (trust nothing)

Record: HEAD SHA, branch, dirty status, package/CI/tests/UI map/latest reports/manifest/provenance.

Run: typecheck, lint, test:discovery, full test, data:check, research:verify-provenance, production build, start app, browser inspect (console/network).

Write: `reports/<ts>-production-readiness-baseline.md` with independent baseline score (Production scorecard §29). Ignore prior 97/100 claims.

Also close Round-6 residuals if still open:
- GAP-07 Bao-18 evidence-gate **automated** regression (inject fail → refuse artifact write)
- Browser/a11y **in CI** (not MCP-only)
- Capability matrix sync

---

## 5. Production objectives (P1–P5)

**P1 Product clarity (≤30s):** what app does/doesn’t; coverage meaning; 10 structured vs 10 random; small exact lift; per-ticket Jackpot unchanged; HOT/COLD = experiments not recommendations.

**P2 Exact probability first:** exact math primary; MC secondary diagnostic; never let noisy MC visually outrank exact without warning.

**P3 Production UX:** discoverable controls, defaults, responsive, keyboard, loading/empty/error/offline/retry, browser-tested.

**P4 Backend reliability:** API hardening, real rate-limit strategy (or honest limitation + coalescing/cache), observability, tracing, structured logs, health/readiness, safe refresh, integrity checks.

**P5 Scientific integrity:** protocol lock, experiment identity (+protocolHash), provenance, anti-peek prospective, reproducibility, immutable historical exceptions, explicit scientific status.

---

## 6. Feature inventory (machine-readable)

For every capability record:
`feature | source_module | engine_status | active_call_path | ui_location | user_action | runtime_output | test_coverage | browser_verification | production_status | scientific_status`

Final statuses only:
`END_TO_END_PRODUCTION | READ_ONLY_PRODUCTION | OPERATOR_GATED_PRODUCTION | INTENTIONALLY_DISABLED | REMOVED`

No meaningful active capability left ENGINE_ONLY / CLI_ONLY / UNKNOWN / UNVERIFIED without intentional production reason.

### Prune (Round 6 absorb)
Classify KEEP_CORE / KEEP_READONLY / KEEP_OPERATOR_GATED / DEMOTE / DELETE.
- DELETE/demote Ranking Score UX promotion; dead copy; competing first-viewport noise.
- KEEP_OPERATOR_GATED: prospective freeze/append, provenance verify CLI.
- KEEP_CORE: Scientific Verdict, Data Status, evidence tables, Experiment Scorecard + chain health, Bao-18, Diagnostics (Ablation+MC), Explorer, Portfolio + exact benchmark, Ticket sim, Profit if non-predictive.

---

## 7. Portfolio UX — 10 vs 30 discoverability

Default 10; engine 1–30. Must show:
`10 vé đang chọn · có thể điều chỉnh từ 1–30 vé`
with `[−] N [+]` (+ optional slider), budget, min/max, live updates to tickets/odds/cost.

Browser: default 10 → 20 → 30; layout OK.

---

## 8. Exact Random vs Projective Benchmark (HIGH)

Implement Exact Benchmark Engine (primary UI).

Projective (pairwise ≤1): exact `P(≥4)`, `P(≥5)`, `P(Jackpot)` via mutually exclusive union math.

N independent random: `1-(1-p)^N` for ≥4, ≥5, Jackpot; distinct-ticket baseline when useful.

UI for N=10, current N, optional 20/30:
Projective / Random / absolute lift pp / relative lift % — for ≥4, ≥5, Jackpot.

Copy: “Projective helps mainly by reducing overlap. It does not make any individual ticket more likely to win.”

MC = Secondary Simulation Check; show n, seed, SE/uncertainty; warn if |MC−exact| > tolerance.

Report: `reports/<ts>-exact-random-vs-projective-benchmark.md`

Hard cap: exact available but primary UI uses noisy MC → max 90.

---

## 9. Portfolio value explanation

Side-by-side 10 Random vs 10 Structured (overlap focus).
What improves: pair coverage, reduced overlap, exact high-tier union.
What does not: per-ticket p, per-ticket EV, next-draw prediction, Jackpot beyond buying more distinct tickets.

---

## 10. Scientific Verdict (production)

Before advanced stats:

```text
Predictive edge: NOT DEMONSTRATED
Portfolio structural advantage: YES — SMALL BUT EXACT
Per-ticket Jackpot probability: UNCHANGED
Prospective evidence: PENDING / SCORED
Recommended prediction strategy: NONE
```

---

## 11. Anti automation-bias

Hide experimental six-number sets by default.
Reveal only after `Hiển thị bộ số thử nghiệm` + disclaimer:
“Đây là output của một rule nghiên cứu, không phải dự đoán được xác nhận.”
Ban: gợi ý mua / recommended / best numbers / predicted numbers.

---

## 12. Monte Carlo policy (project-wide)

Exact available → exact. Else MC.
Every MC panel: sim count, seed, SE/uncertainty, reproducibility, diagnostic vs decision-grade.

---

## 13–14. Prospective + provenance exceptions

Preserve legacy unchained; hash-chain future events; CI verify; UI:

```text
Frozen / Scored / Chained / Legacy / Integrity PASS|FAIL
```

No silent history rewrite. Historical exceptions immutable; `newExceptionCountSinceCutoff`; any new undocumented exception → CI FAIL.

---

## 15–16. Test architecture + mandatory Browser CI

Layers: unit, property (e.g. projective P≥4 ≥ random independent where expected; hash invariance; anti-leak; pairwise ≤1), integration, **Playwright E2E in CI**.

Viewports: 1440×900, 768×1024, 390×844.

Journeys: Research (verdict, data, Bao-18, diagnostics); Portfolio (10/20/30 + exact benchmark + clipboard); Ticket; error injection (missing JSON, refresh fail, 429, corrupted cache, offline); a11y keyboard.

Fail CI on console.error, uncaught, failed required API, hydration error.

Hard cap: no browser E2E in CI → max 89.

Also keep Browser MCP acceptance pack as human/agent evidence, not substitute for CI.

---

## 17–20. Observability, health, rate limit, freshness

Structured logs, request IDs, refresh/upstream latency, parser/validation/rate-limit/client/artifact failures.

`/api/health` (alive); `/api/readiness` (bundled dataset, manifest, required artifacts, protocol lock) — not live Vietlott uptime.

Rate limit: distributed/platform edge if public; else document limitation + coalescing + cache + no refresh storms. Do not pretend single-isolate limiter is enough.

Freshness UX: Fresh / Delayed / Stale / Unknown; never silent stale-as-current.

---

## 21–23. Historical correction, persistence ADR, licensing

Same draw ID + changed numbers → fail closed, record conflict, preserve snapshot, alert, verified reconciliation.

Re-evaluate ADR-005: no DB unless multi-user/auth/saved portfolios/shared research/schedulers require it. Update ADR.

License checklist: no claimed legal clearance without evidence; attribution; distinguish display vs redistribution.

---

## 24–27. Performance, error boundaries, security, deploy safety

Profile before Web Workers; error boundaries so optional artifact failure ≠ blank app; security review (SSRF, XSS, abuse, caps, CSP, deps); deployment checklist + rollback; never deploy if provenance fails.

---

## 28. CI pipeline target

Install → typecheck → lint → unit → property → integration → test discovery → data integrity → provenance → artifact validation → production build → browser E2E desktop → mobile → a11y → smoke.

No layer may silently pass with zero tests.

---

## 29. Production scorecard (/100)

| Dimension | Weight |
|---|---:|
| Probability/algorithm correctness | 10 |
| Scientific integrity | 10 |
| Provenance/reproducibility | 8 |
| Data engineering | 8 |
| Frontend UX | 8 |
| UI discoverability | 6 |
| Backend reliability | 8 |
| Security | 7 |
| Browser E2E | 8 |
| Test architecture | 7 |
| Accessibility | 5 |
| Observability | 5 |
| Performance | 4 |
| Deployment/rollback | 4 |
| Documentation | 2 |
| **TOTAL** | **100** |

### Hard caps
- No browser E2E in CI → max 89
- New provenance inconsistency → max 79
- Misleading predictive claim → max 69
- Silent data corruption → max 49
- Exact exists but primary UI noisy MC → max 90
- Critical feature undiscoverable → max 92
- No production rollback → max 94
- Console/runtime errors / build-test fail → cannot PASS
- Fabricated prospective → AUTOMATIC FAIL

---

## 30. UI IA default (Round 6 A/B — pre-selected unless disproved)

**Winner default: Verdict-first Production IA**
- Tabs: Research / Portfolio / Ticket
- Research: Scientific Verdict → sticky in-page nav (Data, Evidence, Bao-18, Diagnostics, Advanced, Explorer) → progressive disclosure for dense tables
- Capability Inspector = Advanced anchor (first-class), not forced 4th tab unless A/B proves otherwise

Run A/B vs Four-tab only if Verdict-first fails clarity/reachability criteria.

---

## 31. Deliverables

- `reports/<ts>-production-readiness-baseline.md`
- `reports/<ts>-exact-random-vs-projective-benchmark.md`
- `reports/<ts>-production-architecture.md`
- `reports/<ts>-browser-e2e-acceptance.md`
- `reports/<ts>-security-reliability-review.md`
- `reports/<ts>-production-scorecard.md`
- `reports/<ts>-capability-ui-exposure-matrix.md`
- `docs/production-runbook.md` (deploy, rollback, sync, provenance fail, upstream outage, stale, incident)

---

## 32. Red-team (all must be NO, or remediate)

1. Can user mistake 10 as the maximum ticket count?
2. Can MC visually contradict exact probability?
3. Can projective coverage be mistaken for predictive edge?
4. Can HOT/COLD appear recommended?
5. Can actual six-number experimental output create automation bias?
6. Can stale data appear current?
7. Can an old draw silently change?
8. Can provenance drift happen again?
9. Can a new orphan test exist?
10. Can browser regression pass CI unnoticed?
11. Can console errors ship?
12. Can public refresh be abused?
13. Can rate limit be bypassed across isolates?
14. Can an optional artifact failure crash Research?
15. Can mobile hide a disclaimer?
16. Can a keyboard-only user use Portfolio?
17. Can Monte Carlo be mistaken for exact proof?
18. Can Ranking Score appear like AI prediction?
19. Can prospective result be mutated?
20. Can product be marketed beyond evidence?

Any “yes” requires another remediation loop.

---

## 33. Definition of Done

```text
Exact probability benchmark = PASS
Scientific honesty = PASS
Provenance = PASS
Data integrity = PASS
UI discoverability = PASS
Backend reliability = PASS
Browser E2E CI = PASS
Accessibility = PASS
Security = PASS
Observability = PASS
Production build = PASS
Rollback procedure = PASS
Round-6 residuals (GAP-07 automation, matrix sync) = PASS
No unresolved HIGH findings
Production score = 100/100 with no hard cap binding
```

Scientific may remain `NO_DEMONSTRATED_EDGE` + `PROSPECTIVE_EVIDENCE_PENDING`.

Final verdict only: `PRODUCTION_READY` or `NOT_PRODUCTION_READY`.

Also report: HEAD SHA; baseline→final scores; exact lifts at 10/20/30; browser matrix; unresolved real risks only.

---

## 34. Final directive

Optimize for mathematical correctness, scientific honesty, production reliability, user clarity, reproducible evidence — not “looking intelligent.”

Make impossible to miss:

```text
PREDICTION ≠ COVERAGE ≠ RANDOMNESS ≠ RESEARCH QUALITY
```
